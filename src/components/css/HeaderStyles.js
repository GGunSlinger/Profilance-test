const styles = (theme) => ({
    root: {
        flexGrow: 1,
    },
    menuButton: {
        marginRight: theme.spacing(2),
    },
    titleWrap: {
        display: 'flex',
        flexGrow: 1,
    },
    title: {
        marginRight: "15px"
    }
});

export default styles